# 💊 MedTracker
A touchscreen-optimized daily medication tracker built with React.

## Setup
npm install
npm run dev

Edit DEFAULT_CONFIG in src/App.jsx to set your medications and email address.
Run in kiosk mode: open -a "Google Chrome" --args --kiosk http://localhost:5173
